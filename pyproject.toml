[project]
name = "domain-extension"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "cmocean>=4.0.3",
    "emmiai-noether @ git+https://github.com/Emmi-AI/noether.git@803b36fce6a6e46dd0749beed29ea89493c12fc6", # We fix the commit on purpose to avoid backward incompatibility problems
    "ipykernel>=7.2.0",
    "ipympl>=0.10.0",
]
